# Backup Code Encryptor


This program allows you to encrypt your 2FA backup codes (or practically any file) with a password.


Make sure you have the required modules installed for this to work:

```sh
pip install -r requirements.txt
```

